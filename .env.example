# Copy to .env and fill in. NEVER commit the real .env.
# Your Livestorm API token (Settings > Account > Integrations > API in Livestorm).
LIVESTORM_API_KEY=your_livestorm_api_token_here

# Optional: port to run on (defaults to 3000 locally; deploybay sets PORT automatically).
PORT=3000
